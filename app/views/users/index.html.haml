.middle
  %h2 Users
  %p
    - @users.each do |user|
      - if user.id != 1
        %br
        %br
        = link_to(user.name, user)
        %br
        = user.username
        %br
        = user.hometown
    %br
    %br