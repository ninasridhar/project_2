- current_user.subscribed_to.each do |s|
  - s.photos.each do |p|
    .boxI.imagetext
      .imageblur
      = link_to p do
        = image_tag p.uploaded_image.thumb
      %h6
        %span
          = p.name
          %br
          = p.user.name
