= form_for @vote do |f|
  - if @vote.errors.any?
    #error_explanation
      %h2= "#{pluralize(@vote.errors.count, "error")} prohibited this vote from being saved:"
      %ul
        - @vote.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :user_id
    = f.number_field :user_id
  .field
    = f.label :photo_id
    = f.number_field :photo_id
  .field
    = f.label :number_of_votes
    = f.number_field :number_of_votes
  .actions
    = f.submit 'Save'
